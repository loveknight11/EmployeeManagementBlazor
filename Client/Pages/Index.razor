@page "/"
@using EmployeeManagementBlazor.Shared
@using Syncfusion.Blazor.Grids

@*<Syncfusion.Blazor.Spinner.SfSpinner @bind-Visible="SpinnerVisible" Label="Loading Employees ...">

</Syncfusion.Blazor.Spinner.SfSpinner>*@
@*@if (!SpinnerVisible)
{*@
    <SfGrid TValue="Employee" AllowPaging="true" AllowSorting="true">
        <SfDataManager Adaptor="Adaptors.CustomAdaptor" AdaptorInstance="@typeof(EmployeeAdapter)"></SfDataManager>
        <GridSortSettings >
            <GridSortColumns>
                <GridSortColumn Field="@nameof(Employee.EmployeeId)">
                    
                </GridSortColumn>
            </GridSortColumns>
        </GridSortSettings>
        <GridPageSettings PageSize="5" PageCount="3" PageSizes="@PageCountDropDown"></GridPageSettings>
        <GridColumns>
            <GridColumn Field=@nameof(Employee.EmployeeId) HeaderText="Employee ID"></GridColumn>
            <GridColumn Field=@nameof(Employee.FirstName) HeaderText="First Name"></GridColumn>
            <GridColumn Field=@nameof(Employee.LastName) HeaderText=" Last Name"></GridColumn>
            <GridColumn Field=@nameof(Employee.Email) HeaderText="Email"></GridColumn>
        </GridColumns>
    </SfGrid>
@*}*@

    @code {
        public string[] PageCountDropDown { get; set; } = new string[] { "All" ,"5","10","15","20"};
        //public List<Employee> Employees { get; set; }

        //[Inject]
        //public IEmployeeService EmployeeService { get; set; }

        //public bool SpinnerVisible { get; set; }

        //protected override async Task OnInitializedAsync()
        //{
        //    SpinnerVisible = true;
        //    //await Task.Delay(3000);
        //    Employees = (await EmployeeService.GetEmployees()).ToList();
        //    SpinnerVisible = false;
        //}
    }